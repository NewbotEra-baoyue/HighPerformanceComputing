CONFIGS = {
    "cpu_quick": dict(
        d_model=256,
        n_layer=4,
        n_head=4,
        d_ff=1024,
        dropout=0.1,
        batch_tokens=8192,
        max_seq_len=128,
        max_steps=20000,
        warmup_steps=500,
        lr=6e-4,
        grad_clip=1.0,
        train_limit=200_000,
        valid_limit=5000,
        eval_every=1000,
        log_every=100,
        amp=False,
        compile=False,
    ),
    "gpu_full": dict(
        d_model=512,
        n_layer=8,
        n_head=8,
        d_ff=2048,
        dropout=0.1,
        batch_tokens=32768,
        max_seq_len=128,
        max_steps=100_000,
        warmup_steps=2000,
        lr=6e-4,
        grad_clip=1.0,
        train_limit=None,
        valid_limit=10000,
        eval_every=2000,
        log_every=200,
        amp=True,
        # torch.compile 需要 python3-dev 提供的 Python.h;未安装时保持关闭(eager+AMP 已足够快)
        compile=False,
    ),
}
